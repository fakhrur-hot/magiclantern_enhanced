# Python
*.pyc
__pycache__/

# Trained models (regenerated by training pipeline)
*.joblib

# Raw SD card log data -- never commit captured camera logs
logs/*.txt
# ...except the curated sample fixture used by the training pipeline
!logs/sample_unified_log.txt
